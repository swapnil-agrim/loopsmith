---
id: 0001
title: Add an exclamation to the greeting
lane: small
done_when: "test_greeter passes with greet('x') == 'Hello, x!'"
auto_ok: false
status: pending
---

Make `greet()` return `"Hello, <name>!"` (add the trailing exclamation).
Update `test_greeter.py` to assert the new behavior.
